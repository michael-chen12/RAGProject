# Robots.txt for Multi-Tenant RAG Platform
# Allow crawlers to access public pages, block API and auth routes

User-agent: *
Disallow: /api/
Disallow: /auth/
Allow: /

# Sitemap (if you add one in the future)
# Sitemap: https://your-domain.com/sitemap.xml
